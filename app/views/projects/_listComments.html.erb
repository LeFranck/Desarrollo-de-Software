<div class="row">
    <div class="col-md-12">
        <div class="">
            <h1>
                <small class="pull-right">
                    <%= @project.comments.count %>
                    <%= @project.comments.count == 1 ? 'Comentario' : 'Comentarios' %>
                </small>
            </h1>
        </div> 
    </div>
</div>
<div class="row">
    <div class="comments-list">
        <% @project.comments.reverse_each do |comment| %>
            <div class="media">
                <p class="pull-right"><small>
                    <%= comment.created_at.strftime("%d %b %Y, %H:%M") %></small></p>
                <a class="media-left" href="#">
                    <%#/* Agregar aqui imagen del usuario */%>
                    <%#<img src="http://lorempixel.com/40/40/people/1/">%>
                    <%#<%= debug(comment.user.avatar) %>
                    <%#<%= image_tag image_path(comment.user.avatar), size: "20x20", alt: "Imagen no disponible", class: "img-responsive" %>
                </a>
                <div class="media-body">
                    <h4 class="media-heading user_name">
                        <%= link_to comment.user.username, comment.user %>
                    </h4>
                    <%= comment.content %>
                </div>
            </div>
        <% end %>
    </div>
</div>
